/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.githubusercontent.com",
      "localhost",
      "real-cyan-bandicoot-kit.cyclic.app",
    ],
  },
};

module.exports = nextConfig;
